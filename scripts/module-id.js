/**
 * PF2e Exploration Automation
 * scripts/module-id.js
 *
 * Single source of truth for the module ID, shared by every file that
 * is loaded as a real ES module (as opposed to pasted into Foundry as
 * a standalone world Macro, which cannot use `import`).
 */

export const MODULE_ID = "pf2e-exploration-automation";
